const length = prompt("Indiquez une longueur");
const width = prompt ("Indiquez une largeur");

const surface = length * width;

console.log (`Le rectangle de longueur ${length}cm et de largeur ${width}cm a pour surface ${surface}cm².`)
